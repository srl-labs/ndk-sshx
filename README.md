# ndk-sshx

[sshx.io](https://sshx.io) tunneling in SR Linux with [NDK](https://learn.srlinux.dev/ndk/).

Download sshx bin:

```bash
./run download-sshx
```

then run:

```bash
./run deploy-all
```

to start the lab.
